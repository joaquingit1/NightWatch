# Python 3.12+
# Direct dependencies (mirrors pyproject.toml). For pinned installs, use uv.lock with `uv sync`.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
opencv-python-headless>=4.10.0
numpy>=2.0.0
pydantic>=2.9.0
websockets>=13.0,<15
requests>=2.32.0
